# Local dev: relative path + Vite proxy in vite.config.ts
VITE_API_URL=/api/v1

# Production (Vercel) — domain only is enough:
# VITE_API_URL=https://sobat.nodeteam.site
#
# Full URL also works:
# VITE_API_URL=https://sobat.nodeteam.site/api/v1
