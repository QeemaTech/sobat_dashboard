# Local dev: requests go to /api/v1 → Vite proxy → backend origin
VITE_API_URL=/api/v1

# Production build (Vercel): full API URL — see .env.production
# VITE_API_URL=https://sobat.nodeteam.site/api/v1
